# AI Handoff

## 完了したこと

- x86_64 System V ABI 用の最小 cooperative threading / trampoline 教材を実装。
- `st_thread_create()` が TCB と専用スタックを作り FIFO ready queue に追加し、`st_start()` が最初のスレッドへ切り替える。
- `ctx.S` は Intel 構文で `rsp` と callee-saved レジスタを保存・復元する。
- README に初期スタック、context switch、戻りアドレス、Mermaid の制御フロー図を記載。
- trampoline の仕組みを初期スタックの詳細より先に説明し、`ret` と通常の `fn(arg)` 呼出しをつなぐアダプターであることを明記 (コミット a7d8bc9)。
- `*.c`、`*.h`、`*.S` のコメントを easy English に統一。
- README の教材としての改善 4 点を README.md / README_en.md 両方に反映:
  1. 「コンテキストスイッチ」導入と trampoline 概述の重複を解消 (見出しを「trampoline とは何か」に変更)。
  2. 「gdb で観察する」セクションを追加 (trampoline 到着時のアライメント・alignment word・`bt` 不可の観察、yield 中の戻りアドレス観察)。
  3. 「前提知識」セクションと TCB (`struct st_thread`) のフィールド説明を追加。
  4. 目次を追加し、セクション番号の整合を整理。

## 検証状態

- C ソースのコンパイル成功 (OrbStack 経由 `make build`)。
- `git diff --check` 通過。
- README.md と README_en.md の見出し構造が一致していることを確認。
- **gdb セクションは未検証**。`-O0` + frame pointer + sibling-call 無効での標準的挙動に基づいて記述し、具体的な出力値は載せていない。

## 次回の具体的なステップ

1. (推奨) VM に gdb を入れて「gdb で観察する」セクションを実機検証する: `scripts/in-linux.sh x64-linux-env "apt-get install -y gdb"`。`break trampoline` のシンボル解決と `bt` の実際の出力を確認し、必要なら記述を修正する。
2. `git push` はユーザーから明示的に指示された場合のみ実行する。
