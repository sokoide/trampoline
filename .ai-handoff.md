# AI Handoff

## 完了したこと

- プロジェクト内の名称を `trampolin` から `trampoline` へ統一。
  - README タイトル・本文、include guard、Makefile、`.gitignore`、生成バイナリ、gdb コマンドを更新。
  - 生成バイナリ名は `trampoline_sample`。
- README 構造をレビューして改善。
  - 目次に前提知識を追加し、日英 README の相互リンクを追加。
  - 初期スタックの `0` は ABI アライメント用の padding であり、通常経路では使わないことを明記。
  - trampoline が誤って return した場合のみ、その `0` が無効な戻り先になることを説明。
  - gdb 手順を自己完結化し、最初の停止が main → A の初期切替である点を追記。
  - GDB の backtrace 結果を断定せず、バージョン依存の差を記載。

## 検証状態

- OrbStack 経由の x86_64 Linux で `make build` に成功。
- `trampoline_sample` を5秒実行し、A → B → C の FIFO 切替を確認。
- `git diff --check` 通過。
- GDB は VM に未導入のため、README の gdb 出力は未実機検証。

## 次回の具体的なステップ

1. 必要なら VM に gdb を導入し、README の観察手順を実機で確認する。
2. `git push` はユーザーから明示的に指示された場合のみ実行する。
