# AI Handoff

## 完了したこと

- x86_64 cooperative threading の最小サンプルを実装。
- `st_thread_start()` は TCB と専用スタックを作成するだけにした。
- `st_start()` で生成済みスレッドを FIFO ready queue に移し、実行を開始する。
- A、B、C が `st_yield()` で永遠に切り替わる構成にした。
- `ctx.S` は Intel 構文で、`rsp` と callee-saved レジスタを保存・復元する。
- join、終了処理、互換 API、設定 API、独立した ready queue ファイルなど未使用部分を削除。
- README に trampoline、戻りアドレス、初期スタック、context switch の詳細と Mermaid 図を追加。
- `.gitignore` と OrbStack / qemu / native 用 Makefile を追加済み。

## 検証状態

- C ソースのコンパイル確認済み。
- `ctx.S` の x86_64 Intel アセンブリ構文確認済み。
- macOS の OrbStack VM 起動は、OrbStack 側の `vmgr.log` 権限エラーで実行できなかった。

## 次回の具体的なステップ

1. OrbStack の `x64-linux-env` VM が起動できる状態で `make build` を実行する。
2. `make run` で A、B、C の FIFO 切り替えを確認する。
3. 必要なら README の実行出力例を追加する。
