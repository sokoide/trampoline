# AI Handoff

## 完了したこと

- `st_thread_create()` が TCB と専用スタックを作り、FIFO ready queue に追加する最小構造を実装。
- `st_start()` で main から最初の worker へ切り替え、A / B / C が `st_yield()` で永遠に交代する。
- `ctx.S` は Intel 構文で `rsp` と callee-saved レジスタを保存・復元する。
- README に trampoline、初期スタック、戻りアドレス、callee-saved レジスタ、Mermaid の制御フロー図を記載。
- 再レビューで、README の yield 呼出し連鎖、trampoline の引数説明、`_exit()` の理由を正確化。
- スタック図と実行コードの呼出し連鎖が一致するよう、教材ビルドを `-O0`、frame pointer 保持、末尾呼出し最適化無効に変更。
- Makefile がヘッダをコンパイラ入力に渡していた問題を修正し、`st_thread_create(NULL, ...)` を拒否した。
- `ctx.S` / `ctx.h` から単体教材に不要な兄弟教材への参照を削除。

## 検証状態

- macOS から OrbStack の `x64-linux-env` を経由した `make build` に成功。
- `timeout 5 ./trampolin_sample` で A → B → C の FIFO 切替を確認。
- timeout による終了コード 124 は意図した停止。

## 次回の具体的なステップ

1. 必要なら `make run` を対話的に実行し、`Ctrl-C` で停止する。
2. `git push` はユーザーから明示的に指示された場合のみ実行する。
