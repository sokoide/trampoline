# AI Handoff

## 完了したこと

- 教材全体 (README.md / README_en.md / ソース一式) の再々レビューを実施。
  重大な誤りはなし。軽微な 6 件を修正してコミット済み (bd1eadc):
  - `ctx.h`: st_ctx_swap の呼び出し元記述を「st_yield() から」→「st_yield() の
    呼び出し連鎖 (st_yield -> switch_to_next -> st_ctx_swap) 経由」に正確化。
  - `ctx.S`: 手順 1 を "Save rsp and the current callee-saved registers" に修正
    (rsp は callee-saved ではないため、README §4 と表現を統一)。
  - README 日英: `st_init()` の説明を「main のコンテキストを初期化」→
    「main スレッドを実行中スレッド (current) に設定」に変更 (実装に忠実)。
  - README ja: 「yield 以外の任意のタイミングで切替えが起きない」を
    「yield 以外のタイミングでは切り替えが起きない」に修正。
  - README 日英: `snprintf` + `safe_write_str()` (`write(2)` 直呼び) の意図
    (stdio バッファリングという観察対象外の機構を避ける) を「ビルドと実行」節に追記。
  - README ja: 図キャプションの「を16バイト」→「を 16 バイト」に表記統一。
- レビューは完了。残課題なし。
- 直近 2 コミットのレビューで見つかったハンドオフ内の HEAD ハッシュの自己参照を
  修正し、再レビューで未解決の問題がないことを確認。

## 進行中の状態

- ブランチ: `main`。作業ツリーは clean。直前の文書修正 (`bd1eadc`) と
  このハンドオフ更新を含め、すべてコミット済み。
- 動作変更なし。検証済み: `git diff --check` パス、`make build` 成功、
  5 秒実行で `[A] step 0` → `[B]` → `[C]` → `[A] step 1` → `[B] step 1` を確認。
- `.ai-handoff.md` は git 管理を継続 (ユーザー指示でコミットに含めているため)。

## 次回の具体的なステップ

1. `git push` はユーザーから明示的に指示された場合のみ実行する。
2. 任意: VM に gdb を導入し、README の観察手順を実機で確認する
   (`scripts/in-linux.sh x64-linux-env "apt-get update && apt-get install -y gdb"`)。
