# AI Handoff

## 完了したこと

- ユーザー自身の教材リファインをコミット (`289e4a3` "Refine trampoline teaching material"): Makefile を `CFLAGS ?= -O0 -g -fno-omit-frame-pointer -fno-optimize-sibling-calls` に変更、ヘッダをコンパイラ入力から除外、`st_thread_create(NULL, ...)` を拒否、ctx.S/ctx.h から兄弟教材への参照を削除。
- `-O2` ビルドでのスタック構造への影響を逆アセンブルで実証:
  - `-O2` では `ready_push`/`ready_pop` がインライン化され、`st_yield` → `switch_to_next` → `st_ctx_swap` が tail call (`jmp`) の連鎖になる。スタックに残る戻りアドレスは worker が `st_yield` を呼んだ `call` の 1 つだけ。
  - 動作の正しさは変わらない。「`st_ctx_swap` 入口の `rsp` が直近の `call` の戻りアドレスを指す」という不変条件のみが必要なため (ABI ベースの context switch は最適化に堅牢)。
- 上記の実証結果を README §3 末尾に注記として追記しコミット (`7b8ac69` "Note in README that frame layout is a logical model")。図は論理的な呼び出し連鎖であること、教材ビルドのフラグが図と実物の一致と gdb での観察の前提であることを説明。

## 進行中の状態

- ブランチ: `main`。コミット `7b8ac69` まで反映済み、push は未実施。
- 作業ツリーはクリーン。残作業なし。

## 次回の具体的なステップ

1. `git push` はユーザーから明示的に指示された場合のみ実行する。
2. 余裕があれば、`make CFLAGS='-std=c11 -O2 ...'` で最適化版もビルドできることを README の演習課題として提示する (ABI 堅牢性の確認演習)。
