# AI Handoff

## 完了したこと

- x86_64 System V ABI 用の最小 cooperative threading / trampoline 教材を実装。
- `st_thread_create()` が TCB と専用スタックを作り FIFO ready queue に追加し、`st_start()` が最初のスレッドへ切り替える。
- `ctx.S` は Intel 構文で `rsp` と callee-saved レジスタを保存・復元する。
- README に初期スタック、trampoline、戻りアドレス、Mermaid の制御フロー図を記載。
- README §3 を再レビューし、スタック図を「戻りアドレスだけを示す模式図」と明確化。
- `-O2` ではインライン化・末尾呼出しで物理フレームが図と異なり得る一方、ABI に従い `rsp`、callee-saved レジスタ、有効な継続先を復元するため正しく動作することを追記。

## 検証状態

- macOS から OrbStack の `x64-linux-env` を経由した教材用 `make build` (`-O0`) に成功。
- `-O2` 指定でも x64 Linux でビルドし、`timeout 5 ./trampolin_sample` で A → B → C の FIFO 切替を確認。
- その後、教材用の `-O0` ビルドへ戻した。timeout の終了コード 124 は意図した停止。

## 次回の具体的なステップ

1. 必要なら `make run` を対話的に実行し、`Ctrl-C` で停止する。
2. `git push` はユーザーから明示的に指示された場合のみ実行する。
