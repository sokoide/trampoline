# AI Handoff

## 完了したこと

- 教材レビューを実施し、発見問題をすべて修正 (commit `f45cb76` "Fix educational accuracy and simplify scheduler API")。
  - `st_thread_start` を `st_thread_create` に改名 (TCB とスタックを作るだけのため)。
  - pending list を削除し、`st_thread_create` が直接 ready queue へ push。`st_start()` は最初の切替えのみを行う簡素な構造に。
  - `st.c` に教育的コメントを追加: `current = next` を swap 前に設定する理由、trampoline が引数なしで `current` グローバルから self を得る理由、`_exit(1)`(queue 空 = 異常)と `_exit(0)`(fn 完了)の使い分け、exit() を避ける理由。
  - README を修正: §3 の戻りアドレス説明を呼び出し連鎖 (st_yield → switch_to_next → st_ctx_swap) に沿って正確化、sleep(1) がプロセス全体をブロックする点、trampoline と current の関係、実行出力例を追記。
  - `ctx.S` / `ctx.h` の tests/ や preemptive/ への参照を `../threading/completed/preemptive` への兄弟教材参照と明示。
- `main.c` の worker に sleep のブロッキング性のコメントを追加。
- ビルド・実行検証済み: `make build` 成功 (OrbStack `x64-linux-env` 経路。前回の VM 権限エラーは解消)、`make run` で A→B→C の FIFO 切り替わりを 10 秒分確認。

## 進行中の状態

- ブランチ: `main` (commit `f45cb76` までコミット済み、push は未実施)。
- 作業ツリーはクリーンな状態 (`trampolin_sample` バイナリは .gitignore 済み)。
- レビューで指摘した項目はすべて対応済み。残作業なし。

## 次回の具体的なステップ

1. `git push` を実行する (ユーザーの指示があった場合のみ)。
2. 余裕があれば兄弟教材 `../threading/completed/` (cooperative / preemptive / tests) 側の README と本 repo の相互参照リンクを整理する。
3. `make run` の出力が README の実行出力例と一致していることを再確認する (変更後の初回確認は済み)。
