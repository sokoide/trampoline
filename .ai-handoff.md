# AI Handoff

## 完了したこと

- 教材全体 (README.md / README_en.md / ソース一式) のわかりやすさ・正確性を再レビュー。
  重大な誤りはなし。軽微な 4 件を修正した (コミットメッセージ参照):
  - 手順リスト・§2 本文を `st_start()` → `switch_to_next()` → `st_ctx_swap()` の実際の経路に合わせた (日英)。
  - gdb セクションの「`&trampoline` はすでに ret が消費した直下にある」を「現在の `rsp` より 8 バイト下 (ret が消費した位置)」に言い換え (ja)。
  - §4 の「保存レジスタ = callee-saved だから」表現を、rsp は分類上 callee-saved ではないが call をまたいで論理的に復元されるため一緒に保存する、と正確化 (日英 + `ctx.h` コメント)。
  - ja §3 のスタック図を右開放の枠に変え、全角文字による境界線のずれを解消。

## 進行中の状態

- ブランチ: `main`。レビュー修正をコミット済み (直近コミットは `git log -1` で確認)。
- ソースコード (`st.c` / `ctx.S` / `main.c` / 各ヘッダ / Makefile) の動作変更はなし。
- 検証済み: `git diff --check` パス、`make build` (OrbStack `x64-linux-env` 経由) 成功、
  VM 内 4 秒実行で `[A] step 0` → `[B] step 0` → `[C] step 0` → `[A] step 1` の FIFO 出力を確認。
- 未解決の判断: `.ai-handoff.md` が git 管理されている。セッション状態を公開履歴に
  残す意図がなければ `.gitignore` への追加を検討 (ユーザー判断待ち)。

## 次回の具体的なステップ

1. 必要なら VM に gdb を導入し、README の観察手順を実機で確認する
   (`scripts/in-linux.sh x64-linux-env "apt-get update && apt-get install -y gdb"`)。
2. `git push` はユーザーから明示的に指示された場合のみ実行する。
