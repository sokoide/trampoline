# AI Handoff

## 完了したこと

- cooperative threading / trampoline 教材を再レビューし、正確性と用語説明を改善した。
  - `README.md` / `README_en.md` に対象範囲・制約を追加。ABI (Application Binary
    Interface)、CET (Control-flow Enforcement Technology)、MXCSR、OS、FP、AVX、gdb の
    初出説明を加えた。
  - CET Shadow Stack 非対応を明記し、教材用の標準 `CFLAGS` に
    `-fcf-protection=none` を追加した。
  - `struct st_ctx` を 56 バイトから 64 バイトへ拡張し、MXCSR と x87 control word を
    追加。`ctx.S` の `st_ctx_swap` はこれらを `stmxcsr` / `fnstcw` と
    `ldmxcsr` / `fldcw` で保存・復元する。新規スレッドは作成元の FP 制御状態で開始する。
  - スレッド関数が return した場合は、そのスレッドだけでなくプロセス全体が
    `_exit(0)` することを明記した。
  - `safe_write_str()` を `write_all()` に置き換え、部分書込みと `EINTR` を再試行する
    ようにした。
- 検証済み: `git diff --check` 成功、OrbStack の x86_64 Linux で `make build` 成功。
  `objdump` で FP 状態の保存・復元命令とオフセットを確認し、`readelf -n` で CET
  Shadow Stack を要求する ELF プロパティがないことを確認した。

## 進行中の状態

- ブランチ: `main`。
- 今回の教材改善とこのハンドオフはコミット済み。
- `make run` を 5 秒で停止した際、`[A] step 0` → `[B] step 0` → `[C] step 0` →
  `[A] step 1` → `[B] step 1` を確認済み。停止は `timeout` による想定どおりの SIGTERM。

## 次回の具体的なステップ

1. `git push` はユーザーから明示的に指示された場合のみ実行する。
2. 任意: VM に gdb を導入し、README の観察手順を実機で確認する。
